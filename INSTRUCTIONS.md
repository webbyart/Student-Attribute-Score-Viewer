
# คู่มือการติดตั้งและใช้งานระบบดูคะแนนคุณลักษณะนักเรียน (เวอร์ชัน Google Sheets 100%)

ระบบนี้ได้รับการปรับปรุงให้ทำงานร่วมกับ Google Sheets โดยตรงผ่าน Google Apps Script
ในระหว่างที่ยังไม่ได้ติดตั้ง Backend (Google Apps Script) ระบบจะทำงานใน **Mock Mode** อัตโนมัติ เพื่อให้หน้าเว็บไม่ล่มและสามารถทดลองเล่นได้

---

## 🚀 ขั้นตอนการติดตั้ง Backend (Google Apps Script)

เพื่อให้ระบบบันทึกข้อมูลลง Google Sheet จริงๆ คุณต้องทำตามขั้นตอนดังนี้:

1.  **ไปที่ Google Apps Script:**
    *   เข้าเว็บ [https://script.google.com/](https://script.google.com/)
    *   กด "New Project" (โปรเจกต์ใหม่)

2.  **ใส่โค้ด:**
    *   ลบโค้ดเดิมในไฟล์ `Code.gs` ทิ้งทั้งหมด
    *   เปิดไฟล์ `backend/Code.js` ในโปรเจกต์นี้ คัดลอกโค้ดทั้งหมดไปวางใน `Code.gs` ของ Google Apps Script

3.  **ตั้งค่า Sheet ID:**
    *   ในโค้ดบรรทัดที่ 14: `const DEFAULT_SHEET_ID = '...'`
    *   ตรวจสอบว่าเป็น ID ของ Sheet คุณหรือไม่ (ID ที่คุณให้มาคือ `1tidL2kyTpvyPktQjkD5ZTvLIKvWEaIOS6TTmvDVuY6s`)

4.  **สั่งรัน Setup (ครั้งแรก):**
    *   ใน Google Apps Script Editor, เลือกฟังก์ชัน `setupSheet` จากเมนูดรอปดาวน์ด้านบน
    *   กดปุ่ม "Run" (▶)
    *   ระบบจะขอสิทธิ์ (Review permissions) ให้กด Allow (ถ้าเจอหน้าแจ้งเตือนความปลอดภัย ให้กด Advanced -> Go to ... (unsafe))
    *   *ผลลัพธ์: ระบบจะสร้าง Tabs (Students, Teachers, Tasks ฯลฯ) ใน Google Sheet ของคุณให้โดยอัตโนมัติ*

5.  **Deploy เป็น Web App:**
    *   กดปุ่ม **Deploy** (สีน้ำเงินมุมขวาบน) -> **New deployment**
    *   คลิกที่รูปฟันเฟือง (Select type) -> **Web app**
    *   **Description:** `API v1`
    *   **Execute as:** `Me` (อีเมลของคุณ)
    *   **Who has access:** `Anyone` (**สำคัญมาก!** ต้องเลือกอันนี้ ไม่งั้นหน้าเว็บจะดึงข้อมูลไม่ได้)
    *   กด **Deploy**

6.  **นำ URL มาใส่ในแอป:**
    *   คัดลอก **Web app URL** ที่ได้ (จะลงท้ายด้วย `/exec`)
    *   กลับมาที่โค้ด Frontend เปิดไฟล์ `services/api.ts`
    *   นำ URL ไปวางแทนที่ในตัวแปร `API_URL`
    ```typescript
    export const API_URL = 'https://script.google.com/macros/s/......./exec';
    ```

---

## ✅ การใช้งาน
*   **Student Login:** ใช้ Student ID (เช่น `std001`) หรือลงทะเบียนใหม่
*   **Teacher Login:** ใช้ Email: `admin@admin`, Password: `admin123` (หรือลงทะเบียนครูใหม่)
