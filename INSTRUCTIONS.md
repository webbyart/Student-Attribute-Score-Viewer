
# คู่มือการติดตั้งและใช้งานระบบ (ฉบับแก้ไข Error)

## 🚨 วิธีแก้ปัญหา "Exception: คุณไม่ได้รับอนุญาต... UrlFetchApp.fetch"

ปัญหานี้เกิดจาก Google Script ยังไม่ได้รับสิทธิ์ในการเชื่อมต่อ LINE API คุณต้องทำตามขั้นตอนนี้:

1.  **ไปที่ Google Apps Script Editor** ที่คุณวางโค้ดไว้
2.  ดูที่เมนูบาร์ด้านบน เลือกชื่อฟังก์ชัน **`triggerAuth`** จาก dropdown (อาจจะเลือก `doGet` อยู่ ให้เปลี่ยนเป็น `triggerAuth`)
3.  กดปุ่ม **Run (▶)**
4.  จะมีหน้าต่างเด้งขึ้นมาแจ้งเตือน **"Authorization required"** -> กด **Review permissions**
5.  เลือกบัญชี Google ของคุณ
6.  (ถ้ามีหน้าจอแจ้งเตือนสีแดง "Google hasn't verified this app") -> กด **Advanced** -> กด **Go to ... (unsafe)**
7.  กด **Allow** เพื่ออนุญาต
8.  รอจนกว่า Logs จะขึ้นว่า "Authorization Successful!"
9.  **สำคัญ:** กด **Deploy** -> **New deployment** ใหม่อีกครั้ง!

---

## 🚀 ขั้นตอนการติดตั้งครั้งแรก (หรือรีเซ็ต)

1.  **อัปเดตโค้ด:** คัดลอกโค้ดจาก `backend/Code.js` ไปวางทับใน `Code.gs` ทั้งหมด
2.  **รัน Setup:** เลือกฟังก์ชัน `setupSheet` แล้วกด Run (เพื่อสร้างข้อมูลนักเรียน std001, std002 และครู T01, T02)
3.  **Deploy:**
    *   Deploy > New deployment
    *   Type: **Web app**
    *   Execute as: **Me**
    *   Who has access: **Anyone** (**ต้องเลือก Anyone เท่านั้น**)
    *   กด Deploy
4.  **นำ URL ไปใส่ใน App:**
    *   Copy URL ที่ได้
    *   ไปที่ไฟล์ `services/api.ts` แก้ไขบรรทัด `export const API_URL = '...';`

## 🔑 ข้อมูลเข้าระบบ (Default)
*   **นักเรียน:** `std001` / `123456` หรือ `std002` / `123456`
*   **ครู (Admin):** `admin@admin.com` / `123456`
